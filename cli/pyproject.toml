[project]
name = "cli"
version = "0.1.0"
description = "CLI утилита"
dependencies = [
    "typer>=0.12.0",
    "requests>=2.31.0",
    "rich>=13.7.0",
    "pyjwt>=2.8.0",
]

[project.scripts]
cli = "app.__main__:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
